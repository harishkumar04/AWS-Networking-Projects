# Create a VPC

# Create 2 subnets

We will create two subnets in two different AZ

# Create and attach an IGW to the VPC

Add the subnet associations

# Create two EC2 instances one in each subnet

# Create a Target Group

That contains both the EC2 instances


# Create a Load Balancer

And the target group

